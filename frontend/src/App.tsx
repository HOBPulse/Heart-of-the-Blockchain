import Navbar from "./components/Navbar";
import Hero from "./components/Hero";

function App() {
  return (
    <>
      <Navbar />
      <Hero />
    </>
  );
}

export default App;
// This is the main entry point of the React application. It imports the Navbar and Hero components and renders them inside the App component. The App component is then rendered into the root element of the HTML document.